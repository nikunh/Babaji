apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  annotations:
    volume.beta.kubernetes.io/storage-class: puppetstack-block
  labels:
    io.kompose.service: r10k-ssl
  name: r10k-ssl
  namespace: puppetstack
spec:
  storageClassName: puppetstack-block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
